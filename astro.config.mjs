import { defineConfig } from 'astro/config';

// Once your Cloudflare Pages URL (or custom domain) is live, put it here.
// This is only used for things like sitemap/canonical links — the site
// works fine even before you set it.
export default defineConfig({
  site: 'https://fokrul-hasan.pages.dev',
});
